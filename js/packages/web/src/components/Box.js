import styled from "styled-components";
import { color, layout, space, position, background } from "styled-system";

export const Box = styled.div`
  ${space}
  ${layout}
  ${color}
  ${position}
  ${background}
`;
